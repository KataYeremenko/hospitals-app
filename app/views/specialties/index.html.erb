<%= stylesheet_link_tag 'view' %>

<h1> View Specialties </h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Name</th>
      <th>Description</th>
      <th colspan="3">Buttons</th>
    </tr>
  </thead>
  <tbody>
    <% @specialties.each do |specialty| %>
      <tr>
        <td><%= specialty.id %></td>
        <td><%= specialty.name %></td>
        <td><%= specialty.description %></td>
        <td><%= link_to 'View', specialty_path(specialty), class: "button" %></td>
        <td><%= link_to 'Update', edit_specialty_path(specialty), class: "button" %></td>
        <td><%= button_to 'Delete', specialty_path(specialty), method: :delete, data: { confirm: 'Are you sure?' }, class: "button" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<div class="links-container">
  <%= link_to 'Add Specialty', new_specialty_path, class: "bottom-button" %>
  <%= link_to 'Back to Main page', '/', class: "bottom-button" %>
</div>